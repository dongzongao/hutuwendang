---
title: 栈与队列算法题
description: 栈与队列经典算法题解析，涵盖有效括号、最小栈、滑动窗口最大值，含多语言运行与逐步执行流程可视化
---

<script setup>
const validBracketsCodes = {
  javascript: `function isValid(s) {
  const stack = [];
  const match = { ')': '(', ']': '[', '}': '{' };
  for (const c of s) {
    if ('([{'.includes(c)) {
      stack.push(c);
      __trace__("'" + c + "' 入栈", { stack: [...stack] });
    } else {
      const top = stack.pop();
      const ok = top === match[c];
      __trace__("'" + c + "' 匹配栈顶'" + top + "': " + (ok ? '✓' : '✗'), { 匹配: ok });
      if (!ok) return false;
    }
  }
  return stack.length === 0;
}
console.log(isValid("([{}])")); // true
console.log(isValid("([)]"));   // false`,

  java: `import java.util.*;

public class Solution {
    public boolean isValid(String s) {
        Deque<Character> stack = new ArrayDeque<>();
        for (char c : s.toCharArray()) {
            if (c == '(' || c == '[' || c == '{') { stack.push(c); continue; }
            if (stack.isEmpty()) return false;
            char top = stack.pop();
            if (c == ')' && top != '(') return false;
            if (c == ']' && top != '[') return false;
            if (c == '}' && top != '{') return false;
        }
        return stack.isEmpty();
    }
    public static void main(String[] args) {
        Solution s = new Solution();
        System.out.println(s.isValid("([{}])")); // true
        System.out.println(s.isValid("([)]"));   // false
    }
}`,

  python: `def is_valid(s):
    stack = []
    match = {')': '(', ']': '[', '}': '{'}
    for c in s:
        if c in '([{':
            stack.append(c)
        else:
            if not stack or stack[-1] != match[c]:
                return False
            stack.pop()
    return not stack

print(is_valid("([{}])"))  # True
print(is_valid("([)]"))    # False`,
}

const minStackCodes = {
  javascript: `class MinStack {
  constructor() { this.stack = []; this.minStack = []; }
  push(val) {
    this.stack.push(val);
    const min = this.minStack.length === 0 ? val : Math.min(val, this.minStack[this.minStack.length-1]);
    this.minStack.push(min);
    __trace__('push(' + val + '), 当前最小=' + min, { stack: [...this.stack], minStack: [...this.minStack] });
  }
  pop() {
    const v = this.stack.pop(); this.minStack.pop();
    __trace__('pop() → ' + v, { stack: [...this.stack] });
  }
  getMin() { return this.minStack[this.minStack.length-1]; }
}
const ms = new MinStack();
ms.push(-2); ms.push(0); ms.push(-3);
console.log('getMin:', ms.getMin()); // -3
ms.pop();
console.log('pop 后 getMin:', ms.getMin()); // -2`,

  java: `import java.util.*;

class MinStack {
    Deque<Integer> stack = new ArrayDeque<>();
    Deque<Integer> minStack = new ArrayDeque<>();

    public void push(int val) {
        stack.push(val);
        int min = minStack.isEmpty() ? val : Math.min(val, minStack.peek());
        minStack.push(min);
    }
    public void pop() { stack.pop(); minStack.pop(); }
    public int top() { return stack.peek(); }
    public int getMin() { return minStack.peek(); }

    public static void main(String[] args) {
        MinStack ms = new MinStack();
        ms.push(-2); ms.push(0); ms.push(-3);
        System.out.println(ms.getMin()); // -3
        ms.pop();
        System.out.println(ms.getMin()); // -2
    }
}`,

  python: `class MinStack:
    def __init__(self):
        self.stack = []
        self.min_stack = []

    def push(self, val):
        self.stack.append(val)
        min_val = val if not self.min_stack else min(val, self.min_stack[-1])
        self.min_stack.append(min_val)

    def pop(self):
        self.stack.pop()
        self.min_stack.pop()

    def get_min(self):
        return self.min_stack[-1]

ms = MinStack()
ms.push(-2); ms.push(0); ms.push(-3)
print(ms.get_min())  # -3
ms.pop()
print(ms.get_min())  # -2`,
}

const maxSlidingWindowCodes = {
  javascript: `function maxSlidingWindow(nums, k) {
  const deque = [], result = [];
  for (let i = 0; i < nums.length; i++) {
    if (deque.length && deque[0] < i - k + 1) deque.shift();
    while (deque.length && nums[deque[deque.length-1]] < nums[i]) deque.pop();
    deque.push(i);
    if (i >= k - 1) {
      result.push(nums[deque[0]]);
      __trace__('窗口[' + (i-k+1) + '...' + i + '] 最大=' + nums[deque[0]], { 队列值: deque.map(d => nums[d]), 最大: nums[deque[0]] });
    }
  }
  return result;
}
console.log(JSON.stringify(maxSlidingWindow([1,3,-1,-3,5,3,6,7], 3)));`,

  java: `import java.util.*;

public class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int[] result = new int[nums.length - k + 1];
        Deque<Integer> deque = new ArrayDeque<>();
        for (int i = 0; i < nums.length; i++) {
            if (!deque.isEmpty() && deque.peekFirst() < i - k + 1) deque.pollFirst();
            while (!deque.isEmpty() && nums[deque.peekLast()] < nums[i]) deque.pollLast();
            deque.offerLast(i);
            if (i >= k - 1) result[i - k + 1] = nums[deque.peekFirst()];
        }
        return result;
    }
    public static void main(String[] args) {
        System.out.println(Arrays.toString(
            new Solution().maxSlidingWindow(new int[]{1,3,-1,-3,5,3,6,7}, 3)
        ));
    }
}`,

  python: `from collections import deque

def max_sliding_window(nums, k):
    dq, result = deque(), []
    for i, n in enumerate(nums):
        if dq and dq[0] < i - k + 1:
            dq.popleft()
        while dq and nums[dq[-1]] < n:
            dq.pop()
        dq.append(i)
        if i >= k - 1:
            result.append(nums[dq[0]])
    return result

print(max_sliding_window([1,3,-1,-3,5,3,6,7], 3))`,
}
</script>

# 栈与队列算法题

> 核心思想：栈 LIFO 适合处理"最近"关系（括号匹配、单调栈），队列 FIFO 适合 BFS 层序遍历。

---

## 1. 有效括号

遇到左括号入栈，遇到右括号弹出栈顶检查匹配，最后栈为空则合法。

<CodeRunner lang="javascript" :codes="validBracketsCodes"></CodeRunner>

---

## 2. 最小栈

两个栈同步，辅助栈每次压入当前最小值，pop 时同步弹出，getMin O(1)。

<CodeRunner lang="javascript" :codes="minStackCodes"></CodeRunner>

---

## 3. 滑动窗口最大值（单调队列）

双端队列维护单调递减序列（存下标），队头始终是当前窗口最大值。

<CodeRunner lang="javascript" :codes="maxSlidingWindowCodes"></CodeRunner>
